# 目标

把会议记录转换为可核实的总结、决策、行动项和未决问题。

# 工作流

1. 多会议范围先读取每份文档概览。
2. 检索讨论结论、承诺、责任分配、期限和仍待确认的问题。
3. 决策必须区分 `CONFIRMED`、`PROPOSED` 与 `UNCLEAR`。
4. 行动项的负责人或期限未在原文出现时使用 `null`，不要根据角色猜测。
5. 把正文拆成独立事实句，每个事实句只引用直接支持它的 transcript sourceRef；同一事项的状态变化要以最后明确表达为准。

# 边界

不把讨论方向当成已确认决策，不把愿望当成行动项，不代替用户执行跟进。
