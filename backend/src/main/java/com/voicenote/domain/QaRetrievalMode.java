package com.voicenote.domain;

/** The strongest evidence-retrieval mode available for one document snapshot. */
public enum QaRetrievalMode {
    TRANSCRIPT_LOCAL,
    FORMAL_OVERVIEW,
    HYBRID_INDEX
}
