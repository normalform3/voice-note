package com.voicenote.domain;

/** A human-readable role is intentionally separate from the stable ASR speaker id. */
public enum SpeakerRole {
    UNKNOWN,
    INTERVIEWER,
    CANDIDATE,
    PARTICIPANT
}
